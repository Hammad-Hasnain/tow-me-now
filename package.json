{
  "name": "towmenow",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devEngines": {
    "packageManager": {
      "name": "npm",
      "version": "^11.9.0"
    }
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^2.11.2"
  }
}